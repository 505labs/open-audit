[package]
name = "rusty-claude-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[[bin]]
name = "openaudit"
path = "src/main.rs"

# Backward-compat alias retained for one release after the OpenAudit rebrand.
# A tiny shim that prints a stderr deprecation warning (TTY only) and
# delegates to the sibling `openaudit` binary via process::Command. Lets
# integration tests bound on env!("CARGO_BIN_EXE_claw") keep compiling.
[[bin]]
name = "claw"
path = "src/bin/claw_legacy_shim.rs"

[dependencies]
api = { path = "../api" }
commands = { path = "../commands" }
compat-harness = { path = "../compat-harness" }
crossterm = "0.28"
pulldown-cmark = "0.13"
rustyline = "15"
runtime = { path = "../runtime" }
plugins = { path = "../plugins" }
serde = { version = "1", features = ["derive"] }
serde_json.workspace = true
syntect = "5"
tokio = { version = "1", features = ["rt-multi-thread", "signal", "time"] }
tools = { path = "../tools" }

[lints]
workspace = true

[dev-dependencies]
mock-anthropic-service = { path = "../mock-anthropic-service" }
serde_json.workspace = true
tokio = { version = "1", features = ["rt-multi-thread"] }

